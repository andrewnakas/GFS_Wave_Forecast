# GFS Wave Forecast Visualization - Python Dependencies

# Core data fetching
xarray>=2023.1.0
netCDF4>=1.6.0
numpy>=1.24.0
pandas>=2.0.0
requests>=2.31.0
dask>=2023.1.0  # Required for lazy loading large datasets

# Optional: For better GRIB file support
# cfgrib>=0.9.10

# Web server (for local testing)
# No additional dependencies needed - using Python's built-in http.server
